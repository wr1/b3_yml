[project]
name = "b3-yml"
version = "0.1.0"
description = "Example configurations, airfoils and polars for b3m"
readme = "README.md"
authors = [
  { name = "wr1", email = "8971152+wr1@users.noreply.github.com" },
]
requires-python = ">=3.10"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src"]
